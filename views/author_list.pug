extends layout

block content
    h1= title

    ul
        each author in author_list
            li
                a(href=author.url) #{author.name}
                |  nascido em #{author.nascimento_formatada} morto em #{author.data_morte_formatada}
                |  (#{author.lifespan} anos)

        else
            li There are no authors.